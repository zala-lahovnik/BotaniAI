import { StyleSheet } from 'react-native';
// accent color: red
export const global: Record<string, StyleSheet.NamedStyles<any>> = {
  // colors
  color: {
    primary: {
      /* primary color */
    },
  },

  // fonts
  font: {
    primary: {
      /* custom font */
    },
  },
};
